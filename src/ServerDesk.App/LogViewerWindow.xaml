<Window x:Class="ServerDesk.App.LogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Logs"
        Width="1360"
        Height="820"
        MinWidth="1040"
        MinHeight="620"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        Loaded="WindowOnLoaded"
        Closed="WindowOnClosed">
    <Window.Resources>
        <Style x:Key="LogButton" TargetType="Button">
            <Setter Property="Padding" Value="11,7" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Margin" Value="7,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="FilterLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="10,0,6,0" />
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock x:Name="TitleText" FontSize="26" FontWeight="SemiBold" />
                <TextBlock x:Name="EndpointText" Margin="0,5,0,0" Foreground="{DynamicResource MutedTextBrush}" />
            </StackPanel>
            <WrapPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Content="Refresh" Click="RefreshOnClick" Style="{StaticResource LogButton}" Margin="0" />
                <Button Content="Follow" Click="FollowOnClick" Style="{StaticResource LogButton}" />
                <Button Content="Pause" Click="PauseOnClick" Style="{StaticResource LogButton}" />
                <Button Content="Resume" Click="ResumeOnClick" Style="{StaticResource LogButton}" />
                <Button Content="Cancel" Click="CancelOnClick" Style="{StaticResource LogButton}" />
                <Button Content="Export" Click="ExportOnClick" Style="{StaticResource LogButton}" />
            </WrapPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Source" VerticalAlignment="Center" Foreground="{DynamicResource MutedTextBrush}" />
            <ComboBox x:Name="SourceBox" Grid.Column="1" Margin="8,0,0,0"
                      SelectionChanged="SourceBoxOnSelectionChanged" />
            <TextBlock Grid.Column="2" Text="Journal unit" Style="{StaticResource FilterLabel}" />
            <TextBox x:Name="JournalUnitBox" Grid.Column="3"
                     ToolTip="Optional systemd .service filter passed as a tokenized journalctl argument." />
            <TextBlock Grid.Column="4" Text="Remote file" Style="{StaticResource FilterLabel}" />
            <TextBox x:Name="FilePathBox" Grid.Column="5"
                     ToolTip="Absolute remote text log path, for example /var/log/nginx/error.log." />
        </Grid>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="1.2*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Search" VerticalAlignment="Center" Foreground="{DynamicResource MutedTextBrush}" />
            <TextBox x:Name="SearchBox" Grid.Column="1" Margin="8,0,0,0" TextChanged="FilterOnChanged" />
            <TextBlock Grid.Column="2" Text="Severity" Style="{StaticResource FilterLabel}" />
            <ComboBox x:Name="SeverityBox" Grid.Column="3" SelectionChanged="FilterOnSelectionChanged" />
            <TextBlock Grid.Column="4" Text="Identifier" Style="{StaticResource FilterLabel}" />
            <TextBox x:Name="IdentifierFilterBox" Grid.Column="5" TextChanged="FilterOnChanged" />
            <TextBlock Grid.Column="6" Text="Unit" Style="{StaticResource FilterLabel}" />
            <TextBox x:Name="UnitFilterBox" Grid.Column="7" TextChanged="FilterOnChanged" />
        </Grid>

        <Border Grid.Row="3" Margin="0,12,0,10" Padding="10" CornerRadius="7"
                Background="{DynamicResource SurfaceSecondaryBrush}">
            <TextBlock x:Name="StatusText" TextWrapping="Wrap" />
        </Border>

        <DataGrid x:Name="LogGrid"
                  Grid.Row="4"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="1.35*" />
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="0.8*" />
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="0.75*" />
                <DataGridTextColumn Header="Unit" Binding="{Binding SystemdUnit}" Width="1.35*" />
                <DataGridTextColumn Header="Identifier" Binding="{Binding Identifier}" Width="1.1*" />
                <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="0.65*" />
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="4*" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="FooterText" Grid.Row="5" Margin="0,10,0,0"
                   Foreground="{DynamicResource MutedTextBrush}" />
    </Grid>
</Window>
