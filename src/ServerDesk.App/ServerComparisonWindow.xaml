<Window x:Class="ServerDesk.App.ServerComparisonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Loc.GlobalDashboard.ComparisonWindowTitle}"
        Width="1240"
        Height="680"
        MinWidth="900"
        MinHeight="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="{DynamicResource Loc.GlobalDashboard.ComparisonTitle}"
                       FontSize="26"
                       FontWeight="SemiBold" />
            <TextBlock Margin="0,5,0,0"
                       Text="{DynamicResource Loc.GlobalDashboard.ComparisonSubtitle}"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource MutedTextBrush}" />
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,18,0,14"
                Padding="12"
                CornerRadius="8"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1">
            <TextBlock x:Name="TargetsTextBlock"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource MutedTextBrush}" />
        </Border>

        <Border Grid.Row="2"
                Padding="8"
                CornerRadius="10"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1">
            <DataGrid x:Name="ComparisonGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserSortColumns="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      BorderThickness="0"
                      Background="Transparent"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto" />
        </Border>

        <Button Grid.Row="3"
                Margin="0,14,0,0"
                HorizontalAlignment="Right"
                Padding="14,7"
                Content="{DynamicResource Loc.GlobalDashboard.ComparisonClose}"
                Click="CloseOnClick" />
    </Grid>
</Window>
